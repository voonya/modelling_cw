// import { DefaultItem } from '../default-item';
// import { Element } from '../element';

// export interface NextNode<T extends DefaultItem> {
//   value: number;
//   node: Element;
// }

// export abstract class NextElementProvider<T extends DefaultItem> {
//   nextElements: NextNode<T>[];

//   constructor(nextElements: NextNode<T>[]) {
//     this.nextElements = nextElements;
//   }

//   public abstract entryNextElement(exitedItem: T, currentTime: number): void;
// }
